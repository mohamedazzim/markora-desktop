# Contributing

Use Node.js 20+, run `npm install`, and keep `npm run verify` passing. Changes to the preload contract must include main-process validation and tests. Do not add remote content or privileged renderer APIs without a security review.
