# Third-party licenses

Markora uses permissively licensed open-source libraries including Electron (MIT), React (MIT), CodeMirror (MIT), Marked (MIT), DOMPurify (Apache-2.0/MPL-2.0), KaTeX (MIT), Zod (MIT), and Vite (MIT). The definitive, version-specific inventory is generated from `package-lock.json` before release; run `npm ls --all` and retain the output with the release artifacts.
